{
  "name": "cairns-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npx @11ty/eleventy",
    "serve": "npx @11ty/eleventy --serve --port=8081"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
